{{!< default}} {{!-- The tag above means: insert everything in this file into the {body} of the default.hbs template
    --}} <div class="site-header-content">
    {{#if @site.cover_image}}
    {{!-- This is a responsive image, it loads different sizes depending on device
    https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
    <img class="site-header-cover" srcset="{{img_url @site.cover_image size=" s"}} 300w, {{img_url @site.cover_image
        size="m" }} 600w, {{img_url @site.cover_image size="l" }} 1000w, {{img_url @site.cover_image size="xl" }} 2000w"
        sizes="100vw" src="{{img_url @site.cover_image size=" xl"}}" alt="" />
    {{/if}}
    <h1 class="site-title">
        {{#if @site.logo}}
        <img class="site-logo" src="{{img_url @site.logo size=" m"}}" alt="{{@site.title}}" />
        {{else}}
        {{@site.title}}
        {{/if}}
    </h1>
    <p class="site-description">
        Musings on the <a class="eco-environment" href="{{@site.url}}/tag/environment">environment</a>, <a
            class="eco-mental-health" href="{{@site.url}}/tag/mental-health">mental health</a> and what makes my <a
            class="eco-heart" href="{{@site.url}}/tag/heart">heart</a> tick
    </p>
    </div>

    {{!-- The main content area --}}
    <main id="site-main" class="site-main outer">
        {{!-- <div class="eco-content-main inner"> --}}
            {{!-- <div class="eco-sidebar-container">
                <div class="eco-sidebar">
                    <header class="eco-header">

                    </header>
                    <div class="eco-content">
                        <h2>Things That Interest Me</h2>>
                        <br>
                        <a href="">This blogs environmental policy</a>
                    </div>
                    <div class="eco-footer">

                    </div>
                </div>
            </div> --}}
            <div class="posts inner">

                <div class="post-feed">
                    {{#foreach posts}}

                    {{!-- The tag below includes the markup for each post - partials/post-card.hbs --}}
                    {{> "post-card"}}

                    {{/foreach}}
                </div>

            </div>

        {{!-- </div> --}}
    </main>